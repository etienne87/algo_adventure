#include <ctime> 
#include <iostream>
#include <vector>
#include <time.h>
#include <algorithm>
#include "recursion.h"


int main(int argc, char* argv[]){
    srand(0);
    std::cout<<"Recursion Demo"<<std::endl;
    int exo = 0;
    if(argc > 1)
        exo = atoi(argv[1]);



}
